# 記事の候補と、書くときに知っておくこと

`README.md` が公開済みの一覧、`VERIFICATION.md` が各記事の検証記録。
このファイルは**まだ書いていないもの**と、**書く手順で踏んだ落とし穴**を持つ。

最終更新 2026-09-10。

---

## いま止まっているもの

| 記事 | 状態 |
| --- | --- |
| `gmail-plain-text-proportional-font` | **書き上がっていて、公開待ち。** 本文レビュー済み、引用コードは現行実装と一致。あとは `published: true` にして push するだけ |

公開前に1つだけ残っている宿題がある。**「Gmail が実際に比例フォントで描画すること」を、こちらで再現テストしていない。**
著者の実体験と RFC 2646・2007年の記事・現行の拡張機能という傍証に依っている。
スクリーンショットが1枚取れれば、記事で唯一の伝聞部分が実測に変わる（`VERIFICATION.md` の同名セクション参照）。

---

## 書いていない候補

数字は提案順で、優先度ではない。**★は提案時に一番推したもの。**

### ForecastMeta（`~/Projects/ForecastMeta`）

気象予報のマルチプロバイダ評価基盤。`KNOWN_LIMITATIONS.md` と `REVIEW_FINDINGS.md`
に、実測値つきの題材がまとまっている。

1. ★ **JMA AMeDAS の風向インデックスは「1 = 北」ではない**
   `1`=NNE、`16`=N でインデックスがステップ数そのもの。`(index-1)*22.5` と実装して
   全履歴が 22.5° ずれていた。さらに風向は角度なので単純な引き算では測れず、保存済み
   スコアの 17.1%（56,188/329,129）が「絶対誤差 180° 超」という物理的にありえない値を
   持っていた。両方直したら風向 MAE が 86〜90° → 51〜54° になり、**モデルの順位が入れ替わった**。
   気象庁自身の公開時別表と突き合わせて検証している（東京 2026-08-11）のが強い。

2. **cron を `:05` にしたせいで、比較の 99.2% が 10 分ずれていた**
   JMA は10分バケットを自タイムスタンプの約6分後に公開する。`:05` に取ると最新は前の `:50`。
   2,309,881 行が「T の予報 vs T-10分の実測」を比較していた。積算値（降水・日照）は
   60分中50分しか重ならない。`:11` へ変更して解決。締めが効く —
   **公開遅延は契約ではないので、動いたら黙って壊れる**。だから受け取ったバケットを毎回検査する。
   そして**過去は直せない**（ズレていない時刻の観測値をそもそも収集していない）。

3. **エラー時だけ鳴る監視は、壊れたときも黙る** ← *次に着手する予定だったもの*
   `forecastmeta/alerting/`。異常時のみ通知する経路は「異常なし」と「経路が死んだ」が
   同じ見た目になるので、all-clear も毎日送る。件名を固定文字列にしない（いつも同じ件名の
   メールは見られなくなり、そうなると届かないことに気づけない）。閾値は実測で決めている
   （上流タイムアウトは145 runs中43%で発生するので昇格させない／JMAのバケットずれは
   824 runs中1回かつ静かに間違った時刻とペアにするので1回で昇格）。運用して分かった3つ目 —
   回復不能な事実を2週間毎日 warning で叫ぶのは無意味なので2日後に INFO へ落とす。
   チェックが3層（`healthcheck`=ログ / `datacheck`=DB）なのは**ジョブが何も保存せず正常終了しうる**から。

4. **完全に成功したバックアップが「失敗」と報告される**
   `REVIEW_FINDINGS.md #13`。279.9MB を送りリモート sha256 で検証しリネームまで終えた**後**に
   走る `prune` が SSH タイムアウトで例外を投げ、`Backup written` が記録されず ERROR 扱いに。
   後片付けの失敗はバックアップの失敗ではない。リトライ設計が良い —
   `NasUnavailable`（聞けなかった）だけ3回リトライし、**ダイジェスト不一致は絶対にリトライしない**。
   `finalize` はループの外（返事が消えたリネームは再試行すると転送元が消えていて失敗するので
   「読むべき失敗」として残す）。

5. **リードタイムを「初期化時刻」でなく「入手時刻」で測ると決めた話**
   4モデルは同じ6時間毎に初期化されるのに公開までが icon 約4時間・他8〜10時間（388ポーリングで実測）。
   補正しないと決めた判断の記事。代償として公開NWP検証スコアと比較不能になる。副産物として
   `jma_seamless` が日照を72hまでしか出さないので**プールした平均は欠測の多いモデルほど得をする**
   という一般的な罠も入る。

### BurnDepth（`~/Projects/BurnDepth`）

熱傷深度推定モデル。**まだコードは1行も書いていない**、設計と調査だけのプロジェクト。
`DESIGN.md` 2.1 が題材。

6. ★ **公開データセット BIP_US を検証したら、18% のラベルが専門医と食い違った**
   モデルを作る前に「公開データで学習できるか」を検証。唯一条件に合う BIP_US（セビリア大学、94枚）を
   著者自身が再ラベリングし、混同行列を取った。3クラス一致率 **81.6%**。
   **不一致は乱数ではなく系統的**（14件中13件で著者のほうが深い、sign test p=0.0018）。
   「解像度が低いせいでは」という当然の疑いを、**高解像度のみに絞ると一致率がむしろ下がる（75.0%）**
   ことで棄却しているのが検証として質が高い。逆に治療方針が変わる境界（保存 vs 手術）だけなら 90.8%。
   さらに**十分な解像度でも判定不能な画像が13%**ある＝写真のみのモデルの実質的な天井。
   おまけに `DESIGN.md` 4.1 に、使えそうな公開実装（xjtu-mia/BurnAnalysis）が
   重みなし・データなし・ライセンスなし・Linux+CUDA専用で動かない、という再現性の悪い実例がある。

   **これまでの記事と読者層が完全に別（医療×AI）**で、アカウントの露出を広げる効果が期待できる。

### gchat-preop-reminder（`~/Projects/gchat-preop-reminder`）

術前検討リマインダー。**開発は別セッションが持っている**（下記「他セッションとの分担」）。
ここから2本すでに書いた（`gas-chat-api-gcp-disabled`、`gmail-plain-text-proportional-font`）。

7. **ID と手術日の数字マッチング**
   院内IDは先頭ゼロが書かれないので正規化して比較（`2000003` と `0002000003` は同一患者）。
   素朴に `indexOf` すると **`2000003` が `20200003` の一部として「見つかって」しまう**ので、
   `(?<!\d)\d{4,12}(?!\d)` で数字の連なりを丸ごと取り出す。極めつけは
   **IDがたまたま手術日と同じ数字だった場合、その1つの数字に「IDの証明」と「日付の証明」を兼ねさせない**判定。
   `normalize()`（空白除去＋小文字化）と `nfkc()`（幅だけ畳む）を使い分ける理由も入る。
   汎用性が高く、医療を離れても通じる。

8. **祝日でカンファが飛ぶと対象週が動く**
   W1（翌週のカンファ消滅＝前倒しが必要）と W2以降（その週の手術がない）で理由が違う延長ルール。
   主題は**失敗方向の選び方** — 祝日カレンダーが読めなければ祝日を無視して続行し WARNING
   （読めないカレンダーのせいで対象週が毎週倍増するほうが困る）。空いた火曜を無限に探さず
   W5 で止めて「手で確認してください」と言う。おまけの GAS 落とし穴として
   `getDay()` はサーバのクロックを読むので曜日は `Utilities.formatDate(date, tz, 'u')` で取る。

### te-imp-order-check（`~/Projects/te-imp-order-check`）

エキスパンダー・インプラント発注照合。4,590行と大きい。**開発は別セッション。**

9. **「不要」と書かれた97%が、本当に不要とは限らない**
   手術テンプレートの `TE発注` 欄は43件中41件が「不要」（テンプレート既定値が直されないまま）。
   しかしそのうち2件は欄の中に実際の型番が書かれたまま「不要」が残っていた。だから
   **この欄は拾い上げ（screening）には使えるが除外（rule out）には使えない**という非対称設計。
   `HARDWARE_KEYWORDS` に単独の `TE` を入れていた時期に**テンプレートの項目名 `TE発注：` 自体に
   全件マッチして49件中39件が誤検出**という失敗談もある。
   Chat投稿を**証拠として使うが権限としては使わない**（サイズ相談スペースは1投稿に候補が複数並ぶので、
   負けた候補を「食い違い」と報告したら会話そのものを誤りとして報告することになる）設計は
   `gas-chat-api-gcp-disabled` と対になる。

### 7mac（`~/Projects/7mac`、git 未初期化）

10. **写真の日付別自動振り分け**（小ネタ）
    Folder Action だけに頼らず LaunchAgent の3分スイープと二重化、転送中ファイルは
    2秒後のサイズ比較で見送る、共有が未マウントならローカルに作らず何もしない、
    同名衝突は上書きも黙殺もせず連番。他より短くまとまる。

---

## 見送った候補

- **Gmail のプレーンテキスト表示を受信側で等幅に戻す話** — 現象自体は2007年から
  知られていて既出が多い。**送る側の設計**に切り口を変えて `gmail-plain-text-proportional-font`
  として書いた。同じ轍を踏まないよう、提案前に必ず検索して既出量を確かめること。

---

## 書くときの手順と、踏んだ落とし穴

### 記事の型

既存9本はすべて同じ型。**[具体的な症状] — [もっともらしいが実際は違う原因]**。
タイトルは 41〜62 文字、本文は 146〜418 行に収まっている（2026-09-10 実測）。
構成は 症状 → 原因（公式ドキュメントの原文引用）→ 対処 → 周辺の落とし穴 → まとめ → 関連記事リンク。

### 検証の流儀（`VERIFICATION.md`）

**記事本文には検証環境の1行だけを残し、「何を検証していないか」の棚卸しは `VERIFICATION.md` に置く。**
読者は書いてあることを著者が確かめたものとして読むので、記事側で自己申告すると
書いていない箇所まで疑わせることになるため。

未検証の主張は、断定せず**公式ドキュメントの引用として提示する**。

### いちばん高くついた教訓

**ドキュメントを読んで諦めたことを、記事にする前に必ず実行して確かめる。**

`gas-chat-api-gcp-disabled` は当初「Chat の Advanced Service には標準 Cloud プロジェクトが要り、
管理者が GCP を止めているので**読めない**」という記事だった。公開直前に `probeSpaces()` を
実行したら**通ってしまい、記事の結論が丸ごと反転した**。
（ドキュメントの前提条件はボットを作る人向けで、ユーザー認証で読むだけなら不要だった。）

一度公開されたあとで気づいたので、非公開に戻して書き直している。

### このリポジトリは public

`articles/` `books/` `images/` だけが Zenn に出るが、**リポジトリ自体は GitHub で公開**
されている。`VERIFICATION.md` も `BACKLOG.md` もそこで読める。

**他プロジェクトから例を引くときは、実データを持ち込まないこと。** 元プロジェクトが
サニタイズ済みでも、こちらが古いコミットや過去の会話から拾ってくると復活する。実際に
2026-09-10、患者IDの実データと思われる番号を記事と BACKLOG に書いて push する事故を
起こしている（`gchat-preop-reminder` が `テスト ハナコ / ID 2000003` へ差し替えたあとの
番号を、それ以前の値のまま引いていた）。

引くのは**元プロジェクトの現行ファイルにある架空データ**から。

### Zenn の落とし穴（`README.md` にも記載）

- **タイトルは70文字まで。** 超えるとデプロイが `デプロイ中断` で止まり、
  **ランごと中断される**ので、そのpushに含まれた他の記事も同期されない。
- **`published` フィールドがリポジトリ側の真。** ダッシュボードの公開ボタンで公開しても、
  **次の push がそれを上書きする。** 実際に一度、無関係な push で公開中の記事が
  非公開に戻る事故を起こしている。**公開したら frontmatter も `true` にすること。**
- **URL の HTTP ステータスで公開状態は判定できない。**
  403 = Zenn は記事を持っていて未公開、404 = 同期されていない、200 = 公開中。
  ただし `published: false` の下書きも 404 になりうるので、**404 だけでは同期の成否を判定できない。**
  確実なのは https://zenn.dev/dashboard/deploys のデプロイログ。

### 閲覧数の現実（2026-09-09 時点）

7本で合計79PV、公開から約2週間。**この数字では記事の良し悪しは判断できない**（新規アカウントで
フォロワーがいないため）。唯一意味のある差は、CSS/liquid glass が他の3〜6倍あること —
これは記事の出来ではなく**読者の母数**の差。

この種の「特定のエラーを踏んだ人に刺さる」記事は検索でしか読まれず、
Google が長尾クエリで順位をつけるのに数週間〜数か月かかる。**評価は11月頃が妥当。**

母数を増やしたいなら候補 3（監視）と 6（BurnDepth）が間口が広い。
候補 1・9 は狭いが該当者には決定的。

---

## Claude Code on the Web から着手できるか

2026-09-10 に各リポジトリの状態と[公式ドキュメント](https://code.claude.com/docs/en/claude-code-on-the-web)を確認した結果。

**この repo の作業（執筆・推敲・公開）は完全に引き継げる。** 記事も `README.md` も
`VERIFICATION.md` もこのファイルも GitHub にあるため。

題材のソースは別リポジトリにあるので、そこが分かれる。ドキュメントに
"a cloud session can access any repository the connecting GitHub account can see"
とあるので、**同じ GitHub アカウントから見えるリポジトリは clone できる**
（環境のネットワーク設定が許す範囲で）。

| 候補 | ソース | Web から |
| --- | --- | --- |
| 1〜5 | `ForecastMeta`（GitHub, private） | **可** |
| 7〜8 | `gchat-preop-reminder`（GitHub, private） | **可** |
| 9 | `te-imp-order-check`（git だがリモート無し） | **不可**（下記） |
| 6 ★ | `BurnDepth`（**git 未初期化**） | **不可**（下記） |
| 10 | `7mac`（**git 未初期化**） | **不可**（下記） |

### 届かないものを届かせるには

`claude --cloud` はリモートの無いリポジトリを**バンドルしてアップロードする**フォールバックを
持つが、条件が `The directory must be a git repository with at least one commit` なので、
**git 未初期化の `BurnDepth` と `7mac` はバンドルもできない。** また untracked なファイルは
バンドルに含まれない。

`te-imp-order-check` は git リポジトリなのでそのディレクトリから `claude --cloud` すれば
バンドルできるが、**`zenn-content` で開いた Web セッションからは見えない。**

いずれも、**private リポジトリを作って push すれば解決する。**
特に候補 6（BurnDepth）は提案時の一番の推しなので、着手前にこれが要る。

### Web では使えないもの

- **`clasp`** — ローカルの `~/.clasprc.json` で認証しているため。
  `gchat-preop-reminder` の記事で、デプロイ済みの Apps Script と引用コードを
  突き合わせるのに使った。Web ではこの検証ができない。
- **`ListAgents` / `SendMessage`** — 相手はこのマシンのローカルセッションなので届かない。

`curl` での Zenn の公開状態確認（403/404/200）は Web でも可能。

## 他セッションとの分担

| プロジェクト | 開発を持つセッション（2026-09-10 の `ListAgents` 実測） |
| --- | --- |
| `gchat-preop-reminder` | `Preop Reminder (Weekly run)` |
| `te-imp-order-check` | `Te-imp-order-check` |
| `BurnDepth` | `BurnDepth` |
| `ForecastMeta` | `forecastmeta-14` |
| `7mac` | `7mac` |
| `zenn-content` | このセッション（`Zenn`） |

セッション名は再起動で変わる（`ForecastMeta` は `forecastmeta-14` になっていた）。
名前を決め打ちせず、`ListAgents` を実行して現在の一覧から選ぶこと。

**このセッションは記事だけを持ち、開発判断は各プロジェクトのセッションに委ねる。**
一度 `gchat-preop-reminder` の `Config.js` を直接編集しかけて差し戻した経緯がある
（`clasp push` の直前で停止）。

記事を書くために別プロジェクトのコードを読むのは問題ないが、**引用する前に必ず
現行実装と突き合わせること。** 開発セッションはこちらが記事を書いている間も進むので、
記事に載せたコードが公開時点で古くなっていた事故が実際に起きている
（`gmail-plain-text-proportional-font` で `pagedApi()` へのリファクタリング前のコードを引用していた）。

`ListAgents` で一覧、`SendMessage` で連絡できる。
